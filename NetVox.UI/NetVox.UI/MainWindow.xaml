<Window x:Class="NetVox.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="NetVox Radio" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Channel Selector -->
        <TextBlock Grid.Row="0" Grid.Column="0"
               VerticalAlignment="Center"
               Text="Channel:"/>
        <ComboBox x:Name="ChannelCombo"
              Grid.Row="0" Grid.Column="1"
              Width="120"
              Margin="5"/>

        <!-- Profile Buttons -->
        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="BtnLoad" Content="Load Profile" Margin="0,0,10,0"/>
            <Button x:Name="BtnSave" Content="Save Profile"/>
            <Button x:Name="BtnNetwork" Content="Network Settings" Margin="10,0,0,0"/>

        </StackPanel>

        <!-- PTT Buttons -->
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="BtnStart" Content="Start PTT" Margin="0,0,10,0"/>
            <Button x:Name="BtnStop"  Content="Stop PTT"/>
        </StackPanel>

        <!-- Status -->
        <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
            BorderBrush="Gray" BorderThickness="1"
            Padding="10" Margin="0,10,0,0">
            <TextBlock x:Name="TxtStatus" Text="Ready"
                 TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Window>
