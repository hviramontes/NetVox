<UserControl x:Class="NetVox.UI.Controls.ToastHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:NetVox.UI.Controls"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="300"
             IsHitTestVisible="False">

    <UserControl.Resources>
        <controls:ToastKindToBrushConverter x:Key="KindBrush"/>
    </UserControl.Resources>

    <!-- Top-right overlay container -->
    <Grid HorizontalAlignment="Right"
          VerticalAlignment="Top"
          Margin="0,16,16,0"
          IsHitTestVisible="False">
        <ItemsControl x:Name="Items"
                      ItemsSource="{Binding Toasts, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"
                                IsHitTestVisible="False"
                                Margin="0,0,0,0"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="8"
                            Margin="0,0,0,8"
                            Padding="12"
                            Background="{StaticResource SurfaceAlt}"
                            BorderBrush="{StaticResource Amber600}"
                            BorderThickness="1.2"
                            Opacity="{Binding Opacity}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Little badge by severity -->
                            <Ellipse Width="10" Height="10"
                                     VerticalAlignment="Center">
                                <Ellipse.Fill>
                                    <Binding Path="Kind" Converter="{StaticResource KindBrush}"/>
                                </Ellipse.Fill>
                            </Ellipse>

                            <TextBlock Grid.Column="2"
                                       Text="{Binding Message}"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource TextPrimary}"
                                       MaxWidth="380"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
