<UserControl x:Class="NetVox.UI.Views.LoggingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Logging &amp; Diagnostics" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,6,0,6">
            <CheckBox x:Name="ChkVerbose" Content="Enable verbose logging" FontSize="14" VerticalAlignment="Center" />
            <TextBlock Text="   Retention (days):" Margin="16,0,4,0" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtRetention" Width="60" Text="10" VerticalContentAlignment="Center" />
            <Button x:Name="BtnApply" Content="Apply" Margin="12,0,0,0" Padding="14,6" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,6,0,6">
            <Button x:Name="BtnOpenFolder" Content="Open Logs Folder" Padding="12,6" />
            <Button x:Name="BtnOpenToday" Content="Open Today's Log" Margin="8,0,0,0" Padding="12,6" />
            <Button x:Name="BtnWriteTest" Content="Write Test Line" Margin="8,0,0,0" Padding="12,6" />
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="LblHint" Text="Changes are saved to your profile." Foreground="Gray" />
    </Grid>
</UserControl>
